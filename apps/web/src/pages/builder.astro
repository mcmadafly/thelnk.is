---
// The v0.2 builder was re-homed into the admin shell (AFR-150).
// Keep /builder as a permanent entry point that lands on the new editor.
export const prerender = false;
return Astro.redirect('/admin/appearance');
---
